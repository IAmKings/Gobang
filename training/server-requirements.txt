# Install a CUDA-enabled PyTorch build separately from the official PyTorch
# index that matches the server driver. The upper bounds avoid the upstream
# NumPy 2.x API incompatibility without forcing a specific CUDA wheel.
torch>=2.1,<3
numpy>=1.24,<2
PyYAML>=6,<7
tqdm>=4.66,<5
pygame>=2.6,<3
wandb>=0.19,<1
